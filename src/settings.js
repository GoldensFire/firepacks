// Настройки и таблицы, которые нужны и сайту, и его двойнику на Cloudflare.
//
// Отделены от config.js не ради порядка, а по необходимости: config.js читает
// секреты из папки data и потому тянет за собой node:fs, а на Workers никакого
// fs нет вовсе (см. cf/). Сюда вынесено ровно то, что нужно обеим половинам, —
// пороги, по которым считается сложность, и названия, которые уходят в интерфейс.
// Всё остальное (пути, ключи, ручки индексатора) осталось в config.js: Worker
// про них не спрашивает, и таскать их за собой ему незачем.
//
// config.js подмешивает этот объект в config целиком, поэтому в самом проекте
// ничего не изменилось: как было config.minGamesForDifficulty, так и осталось.

export const settings = {
	/**
	 * Сколько вход живёт БЕЗ посещений, дни. Каждое обращение к сайту отодвигает
	 * этот срок заново (см. currentUser), поэтому того, кто заходит хотя бы раз
	 * в пару месяцев, не выкидывает никогда.
	 *
	 * Раньше срок был жёстким: тридцать дней от входа и ни днём больше, сколько
	 * бы человек ни пользовался сайтом. Выглядело это как «сайт сам разлогинивает»,
	 * потому что так оно и было.
	 */
	sessionDays: 60,

	/**
	 * Насколько куку выдают вперёд, дни. Больше, чем живёт сама сессия, и это
	 * не описка: сроков тут два, и они про разное.
	 *
	 * Настоящий срок один — тот, что записан у сессии в базе, и его отодвигает
	 * каждое посещение. Кука же поставлена один раз, и переставить её можно только
	 * вместе с ответом — то есть надо угадать, каким именно ответом. Проще выдать
	 * её сразу надолго: протухшая в базе сессия всё равно никого не пустит, а вот
	 * протухшая раньше времени кука выкидывает того, кто заходил вчера.
	 *
	 * 400 дней — не круглое число, а потолок: больше браузеры всё равно обрезают.
	 */
	cookieDays: 400,

	/**
	 * Не отодвигать срок сессии чаще, чем раз в столько дней. Без этого условия
	 * каждое открытие страницы означало бы запись в базу — а на Cloudflare это
	 * ещё и расход по тарифу на ровном месте.
	 */
	sessionRenewDays: 1,

	/**
	 * Со скольких оценок показывать средний балл. До этого порога у пака видно
	 * только число проголосовавших: средним по двум-трём оценкам верить нельзя,
	 * а показанное число живёт своей жизнью и запоминается.
	 */
	minRatingsForScore: 10,

	/** Куда ведёт кнопка «Играть». */
	playerUri: 'https://sigame.vladimirkhil.com/',

	/**
	 * Сколько игр должно быть у пака, чтобы показывать сложность.
	 * При 100 оценку получает примерно каждый десятый пак, при 30 — каждый пятый.
	 * Уровни уже разобранных паков пересчитываются при запуске, переиндексация не нужна.
	 */
	minGamesForDifficulty: 100,

	/**
	 * Сколько всего вопросов должно быть показано по паку. Бывают паки, которые запускали
	 * полсотни раз, но почти не играли — там процент случаен и оценке верить нельзя.
	 */
	minShownForDifficulty: 300,

	/**
	 * Границы уровней сложности в процентах вопросов, на которые решились ответить.
	 * Доля правильных ответов для этого не годится: она почти у всех паков около 60%,
	 * потому что на трудный вопрос никто не жмёт и в этот процент он не попадает.
	 */
	difficultyThresholds: {
		easy: 65,   // больше 65% — лёгкий
		medium: 45, // 45..65 — средний
		hard: 30,   // 30..45 — сложный
		// меньше 30 — очень сложный
	},

	/**
	 * Доля правильных ответов, ниже которой пак считается на ступень сложнее.
	 * Отвечать-то на его вопросы решаются охотно — потому он и попадает в «лёгкие», —
	 * но отвечают мимо: за столом такой пак ощущается труднее, чем выглядит по
	 * одному проценту попыток. Ступень всего одна и ниже «очень сложного» не уходит.
	 */
	hardRightPercent: 45,

	/** С какой доли вопросов пак считается тематическим, а не солянкой. */
	topicThreshold: 0.6,

	/**
	 * С какой доли музыкальных вопросов пак считается музыкальным. Порог отдельный,
	 * потому что музыка не спорит с остальными тематиками: пак может быть
	 * одновременно аниме-паком на 100% и музыкальным на 90%.
	 */
	musicThreshold: 0.6,

	/**
	 * Со скольких тем франшиза считается повтором. Одна тема по «Наруто» в аниме-паке —
	 * это просто тема, а четыре подряд — уже перекос, ради которого всё и считается.
	 */
	franchiseMinThemes: 2,

	/**
	 * Какую часть тем пака должен занимать повтор, чтобы им быть. Ноль — не считать
	 * от размера пака вовсе: повтором остаются те же franchiseMinThemes темы,
	 * сколько бы тем в паке ни было.
	 *
	 * Считалось от размера, и рассуждение было такое: двух тем мало, когда тем
	 * тридцать, а аниме-пак-угадайка называет за игру полторы сотни тайтлов,
	 * и любые два совпадения в нём случайны. Десятая часть тем, но не меньше
	 * franchiseMinThemes: на шесть тем — две, на тридцать — три, на шестьдесят —
	 * шесть.
	 *
	 * На деле так терялось ровно то, ради чего повторы и смотрят. Пак 2414
	 * («О моём перерождении в гриб») — 38 тем, порог четыре; «Реинкарнация
	 * безработного» стоит в двух темах, «Атака титанов» в двух, и обе пары
	 * выброшены. На карточке — «повторов нет», а человек, открывший пак, видит
	 * их сразу. Совпадение двух тем случайным не бывает: тем в паке хоть шесть,
	 * хоть шестьдесят, а вернуться к одному и тому же дважды — уже возвращение.
	 *
	 * Если списки станут шумными — поднимать сюда, а не в franchiseMinThemes:
	 * счёт от размера пака остался на месте и включается любым числом больше нуля.
	 */
	franchiseThemeShare: 0,

	/**
	 * С какой доли вопросов франшиза считается преобладающей: тогда она выносится
	 * на карточку отдельным ярлыком («половина пака — это одно и то же»).
	 */
	franchiseDominantShare: 0.3,

	/**
	 * Когда повторов в паке столько, что об этом стоит сказать цветом.
	 *
	 * Число повторяющихся тем на карточке само по себе ничего не решает: десять
	 * повторов в паке на четыре сотни вопросов — это обычный большой пак, а те же
	 * десять в паке на полсотни означают, что треть вечера уйдёт на одно и то же.
	 * Поэтому считается доля вопросов пака, приходящаяся на повторы, и по ней
	 * число красится: до первого порога — как обычно, после — предупреждением,
	 * после второго — тревогой.
	 *
	 * Первый порог заодно отвечает на вопрос фильтра «паки с малым количеством
	 * повторов»: малым считается ровно то, что цветом не помечено.
	 */
	repeatWarnShare: 0.1,
	repeatAlarmShare: 0.2,

	/**
	 * То же самое для спецвопросов, и пороги ниже вдвое: аукцион и кот в мешке
	 * ломают ход игры сильнее, чем повтор темы, — двадцатая часть пака в них
	 * это уже заметно, а шестая означает, что играется он совсем иначе.
	 *
	 * Первый порог так же служит фильтру «паки с малым количеством спецвопросов».
	 */
	specialWarnShare: 0.05,
	specialAlarmShare: 0.15,

	/**
	 * С какой доли вопросов пак считается паком про одно — и получает свой,
	 * дополнительный тип: «пак по Вархаммеру», «пак по футболу», «пак по Гарри
	 * Поттеру». Пяти основных тематик для таких паков не хватает: по ним пак про
	 * футбол — «солянка» (спорт живёт в «прочем», и ни одна тематика порога
	 * не берёт), а пак по Вархаммеру — просто «игропак», каких сотни.
	 *
	 * Считается по предметам тем — и по произведениям, и по областям знаний:
	 * тип получает самый частый предмет пака, если он занимает не меньше этой
	 * доли вопросов. Область в повторы не идёт (пак, где есть тема про столицы
	 * и тема про реки, дважды ни к чему не вернулся), а тип пака даёт наравне
	 * с произведением: пак целиком про футбол — это «пак по футболу», и сказать
	 * о нём так и точнее, и полезнее, чем «солянка» (см. computeAreas
	 * в src/topics.js). Порог вдвое выше, чем у преобладающей франшизы: там
	 * вопрос «к чему пак возвращается», а здесь — «про что он весь», и треть
	 * вопросов на такое не отвечает.
	 */
	subjectPackShare: 0.5,

	/**
	 * Сколько дополнительных типов показывать в колонке фильтров. Их столько же,
	 * сколько в базе предметов, — то есть сотни, — а колонка не список всего
	 * на свете: в ней смотрят, чего много. Остальное по-прежнему находится
	 * нажатием на сам предмет в карточке.
	 */
	subjectLimit: 40,

	/**
	 * С какой доли вопросов пак считается паком про одну вселенную — и называется
	 * ею прямо в ярлыке: не «Кинопак», а «Кинопак (Гарри Поттер)».
	 *
	 * Это не то же самое, что subjectPackShare, хотя порог и старше. Тот отвечает
	 * на вопрос «про что пак вообще» и годится паку, где половина вопросов про
	 * футбол, а половина про что попало. Здесь речь о другом: пак целиком, без
	 * остатка, состоит из одной тематики и одного произведения — тогда «Кинопак»
	 * само по себе не говорит ничего нового, а «Кинопак (Гарри Поттер)» говорит всё.
	 *
	 * Ровной сотни не требуем: одна тема про что-то постороннее среди тридцати
	 * не делает пак солянкой, а по строгому равенству такие паки не находились бы
	 * вовсе.
	 */
	universePackShare: 0.9,

	/**
	 * С какой доли вопросов пака вид «прочего» стоит называть вслух. Категория
	 * «Прочее» честна, но пуста: пак на треть про стримеров и пак на треть про
	 * Вторую мировую в ней выглядят одинаково. Модель называет вид каждой такой
	 * темы (см. OTHER_KINDS), а здесь решается, с какой доли его писать рядом
	 * с «Прочим»: «Прочее: стримеры, история».
	 */
	otherKindShare: 0.1,

	/** Сколько видов «прочего» показывать: дальше третьего это уже перечисление всего подряд. */
	otherKindLimit: 3,

	/**
	 * С какой доли вопросов пака жанр стоит называть вслух.
	 *
	 * Тип пака отвечает на вопрос «откуда вопросы», и у тематического пака ответ
	 * на него известен заранее: в музпаке музыка, в аниме-паке аниме. Играются же
	 * два музпака совершенно по-разному, если в одном русский рэп, а в другом
	 * опенинги нулевых, — и вот этого не видно было ниоткуда.
	 *
	 * Доля считается от всего пака, а не от одной его тематики: вопрос, на который
	 * отвечает жанр, — «стоит ли писать это на карточке».
	 *
	 * Десятая часть, а не пятая, как было сначала. Пятая отсекала слишком много:
	 * жанров в списке по десятку, тем в паке тридцать, и пак, где хоррора ровно
	 * три темы, оставался без единой подписи — хотя три темы хоррора это ровно то,
	 * что человек хочет знать до игры. Столько же (десятая часть) берут и виды
	 * «прочего» рядом, и по той же причине.
	 */
	genreShare: 0.1,

	/** Сколько жанров показывать: остальное полоска сводит в «прочее». */
	genreLimit: 6,

	/**
	 * С какой доли носитель стоит показывать отдельным куском верхней полоски
	 * (см. FORMS). Порог низкий и не такой, как у жанров, нарочно: жанров
	 * в списке десяток, а носителей два-три, и вопрос к ним один — «чего тут
	 * сколько». Манга-пак, где манхвы двадцатая часть, — это пак с манхвой,
	 * и умолчать о ней значит ответить «манга целиком», то есть соврать.
	 */
	formShare: 0.03,

	/**
	 * Какую часть своей тематики должны покрывать названные носители, чтобы
	 * верхняя полоска вообще делилась. Причина та же, что у десятилетий:
	 * разбивка, посчитанная по одной десятой кинопака, врала бы с уверенным
	 * видом — «сериальный пак», где сериалы это две темы из тридцати.
	 */
	formCoverage: 0.5,

	/**
	 * С какой доли пака вид «прочего» становится собственным типом пака.
	 *
	 * Половина — то же самое число, по которому пак называется паком про одно
	 * (subjectPackShare), и по той же причине: до половины это «солянка,
	 * в которой много спорта», после — «спортпак, в котором попадается прочее».
	 * Разница не в оттенке: под спортпак собирается своя компания, и обещать
	 * его паку, где спорта треть, нельзя.
	 */
	kindPackShare: 0.5,

	/**
	 * С какой доли пака десятилетие стоит показывать отдельным куском полоски.
	 *
	 * Порог низкий нарочно, и это не то же самое, что жанр. Жанров в списке
	 * десяток, и каждый из них — отдельная мысль о паке; десятилетий в живом
	 * паке три-четыре, и вопрос к ним один: «когда это всё вышло». Пак, где
	 * девяностых ровно двадцатая часть, — это пак с девяностыми, и отсекать
	 * их значило бы отвечать «нулевые и десятые», то есть не отвечать вовсе.
	 */
	decadeShare: 0.03,

	/** Сколько десятилетий показывать: остальное полоска сводит в «прочее». */
	decadeLimit: 9,

	/**
	 * Какую часть пака должны покрывать названные годы, чтобы полоску
	 * десятилетий вообще показывать.
	 *
	 * Год модель называет не у всего: у темы про мемы или про столицы его нет
	 * и быть не должно. Полоска, посчитанная по одной десятой пака, врала бы
	 * с уверенным видом — «пак нулевых», где нулевые это две темы из тридцати.
	 */
	decadeCoverage: 0.25,

	/**
	 * То же самое для полоски происхождения: с какой части пака её можно
	 * показывать. Порог тот же и по той же причине — считается она по тем же
	 * названным произведениям и исполнителям.
	 */
	originCoverage: 0.25,

	/**
	 * С какой доли названного происхождение стоит называть словами.
	 *
	 * Кусков в полоске теперь шесть, и подписывать все — значит писать «2%
	 * Казахское» у пака, где казахского один вопрос из полусотни. Десятая часть
	 * (столько же, сколько у жанров и видов «прочего») отделяет «пак наполовину
	 * советский» от случайной песни: до порога кусок в полоске остаётся, а вот
	 * в подписи под ней его нет.
	 */
	originShare: 0.1,
};

/**
 * Откуда родом названное в паке.
 *
 * Вопрос этот стоит только у музыки и кино, и стоит он там всегда: «музпак» —
 * это и сборник русского рэпа, и сборник западной эстрады, а играются они
 * совсем по-разному и разными людьми. Жанр на него не отвечает — рок бывает
 * и наш, и не наш, — а тематика тем более.
 *
 * Раньше здесь было два ответа: «наше» и «зарубежное». Двух мало. «Наше»
 * складывало в одну кучу советское кино, нынешнюю российскую эстраду,
 * «Океан Ельзи» и «Скриптонита» — а это четыре разных пака, и собираются
 * под них разные компании. Поэтому своим куском названо каждое: советское
 * отдельно от российского (это не страна, а время, и «Ну, погоди!» с новым
 * сериалом ТНТ рядом не стоят), а постсоветское поделено по странам.
 *
 * «Иностранное» — всё остальное, и оно последнее нарочно: порядок ключей
 * это порядок кусков в полоске, и «иностранное» в ней замыкающее.
 */
export const ORIGINS = {
	ru: 'Российское',
	su: 'Советское',
	by: 'Белорусское',
	kz: 'Казахское',
	ua: 'Украинское',
	foreign: 'Иностранное',
};

export const ORIGIN_KEYS = Object.keys(ORIGINS);

/**
 * У каких паков показывать, откуда родом названное. У остальных вопрос
 * бессмысленный: аниме японское всё, и полоска в 100% ничего не сообщает.
 *
 * Музыкальные паки сюда попадают и по доле музыки, а не только по ярлыку:
 * аниме-пак с опенингами числится аниме-паком, но музыка в нём главная
 * (см. isMusical в src/topics.js).
 */
export const ORIGIN_TOPICS = ['music', 'movies', 'cartoons'];

/**
 * У каких паков показывать разбивку по десятилетиям. У «прочего» и солянки
 * её нет: год выхода бывает у произведения, а у вопроса про столицы или
 * про химию его нет.
 */
export const DECADE_TOPICS = ['anime', 'manga', 'games', 'movies', 'cartoons', 'books', 'comics', 'music'];

/** Раньше этого десятилетия всё сводится в один кусок: «до 1950-х». */
export const DECADE_MIN = 1950;

/** Имя десятилетия для подписи: 1990 → «1990-е», DECADE_MIN → «до 1950-х». */
export function decadeName(decade) {
	return decade <= DECADE_MIN ? `до ${DECADE_MIN}-х` : `${decade}-е`;
}

/**
 * Жанры внутри тематики: чем один музпак отличается от другого.
 *
 * Спрашиваются только у паков, которые чем-то одним и являются (см. topicThreshold):
 * у солянки жанр называть не от чего — там и тематики-то нет. Список свой у каждой
 * тематики и закрытый, как и виды «прочего»: открытый превратился бы в тысячу
 * написаний одного и того же, а нужен он затем, чтобы куски складывались в доли.
 *
 * Порядок ключей — это порядок старшинства, и он не случаен. Жанры пересекаются
 * почти всегда: исекай — это ещё и фэнтези, и приключения, и нередко комедия.
 * Модели сказано брать первый подходящий сверху, поэтому наверху стоит то, что
 * про произведение говорит больше всего («исекай»), а внизу — то, что подходит
 * почти ко всему («драма», «повседневность»).
 *
 * question — та самая строка, которой полоска подписана на карточке.
 */
export const GENRES = {
	anime: {
		question: 'Какой жанр аниме?',
		list: {
			isekai: 'исекай',
			// Меха и махо-сёдзё стоят наверху рядом с исекаем и по той же причине:
			// это не оттенок, а устройство произведения целиком. «Евангелион» —
			// это драма, фантастика и сёнэн разом, но играется он как меха;
			// «Сейлор Мун» — романтика, комедия и боевые, а на деле махо-сёдзё.
			// Ниже по списку оба тонули: их утаскивали общие жанры, стоящие
			// рядом почти с каждым тайтлом
			mecha: 'меха',
			mahou: 'махо-сёдзё',
			sport: 'спорт',
			detective: 'детектив и триллер',
			horror: 'хоррор и мистика',
			shounen: 'сёнэн и боевые',
			magic: 'магия и фэнтези',
			scifi: 'фантастика',
			romance: 'романтика и ромком',
			comedy: 'комедия',
			slice: 'повседневность',
			drama: 'драма',
			classic: 'классика и старое аниме',
		},
	},
	games: {
		question: 'Какой жанр игр?',
		list: {
			mmo: 'онлайновые и MMO',
			esports: 'киберспорт',
			shooter: 'шутеры',
			rpg: 'ролевые',
			strategy: 'стратегии',
			survival: 'песочницы и выживание',
			horror: 'хорроры',
			fighting: 'файтинги',
			racing: 'гонки',
			platformer: 'платформеры',
			quest: 'квесты и визуальные новеллы',
			simulator: 'симуляторы',
			tabletop: 'настольные',
			mobile: 'мобильные',
			retro: 'ретро и классика',
			indie: 'инди',
		},
	},
	movies: {
		question: 'Какой жанр кино?',
		list: {
			superhero: 'супергерои',
			horror: 'ужасы',
			detective: 'детективы и триллеры',
			scifi: 'фантастика',
			fantasy: 'фэнтези',
			action: 'боевики',
			soviet: 'советское кино',
			documentary: 'документальное',
			comedy: 'комедии',
			romance: 'мелодрамы',
			drama: 'драмы',
			// «Сериалы» жанром здесь больше нет, и это не потеря, а разделение
			// двух разных вопросов. Сериал — не жанр, а длина: он бывает и комедией,
			// и детективом, и фантастикой, — а стоя в одном списке с ними, он их
			// собой и вытеснял: у кинопака по «Сообществу» весь жанр выходил
			// «сериалы 60%», и о чём этот пак, полоска не говорила вовсе.
			// Теперь кино и сериалы делятся своей полоской (см. FORMS ниже)
		},
	},
	cartoons: {
		question: 'Какой жанр мультиков?',
		list: {
			superhero: 'супергерои',
			soviet: 'советские мультики',
			disney: 'Disney и классика студий',
			adult: 'взрослая анимация',
			fairy: 'сказки и фэнтези',
			adventure: 'приключения',
			comedy: 'комедии',
			kids: 'детские',
		},
	},
	books: {
		question: 'Какой жанр книг?',
		list: {
			poetry: 'поэзия',
			detective: 'детективы',
			horror: 'ужасы и мистика',
			fantasy: 'фэнтези',
			scifi: 'фантастика',
			children: 'детские книги',
			nonfiction: 'нон-фикшн',
			classic: 'классика',
			modern: 'современная проза',
		},
	},
	comics: {
		question: 'Какой жанр комиксов?',
		list: {
			superhero: 'супергерои',
			horror: 'ужасы и мистика',
			detective: 'детективы и нуар',
			scifi: 'фантастика',
			fantasy: 'фэнтези',
			european: 'европейские комиксы',
			webcomics: 'веб-комиксы и стрипы',
			indie: 'инди и авторские',
			comedy: 'юмор',
			drama: 'драма',
		},
	},
	manga: {
		question: 'Какой жанр манги?',
		list: {
			isekai: 'исекай',
			mecha: 'меха',
			mahou: 'махо-сёдзё',
			sport: 'спорт',
			detective: 'детектив и триллер',
			horror: 'хоррор и мистика',
			shounen: 'сёнэн и боевые',
			magic: 'магия и фэнтези',
			scifi: 'фантастика',
			romance: 'романтика и ромком',
			comedy: 'комедия',
			slice: 'повседневность',
			drama: 'драма',
			// Манхвы, маньхуа и вебтунов в жанрах больше нет. Жанром они и не были:
			// это носитель и страна, а не то, про что вещь. Стоя в общем списке,
			// они выигрывали у настоящих жанров всё: манга-пак по корейским
			// вебтунам получал подпись «манхва, маньхуа и вебтуны 100%» — то есть
			// не получал никакой. Место им во второй полоске (см. FORMS ниже),
			// а здесь остались одни жанры
			classic: 'классика манги',
		},
	},
	/*
	 * Виды спорта. Список этот стоит наособицу: спорт — не одна из тематик,
	 * которые модель делит между собой (вопрос про футбол — это «прочее»),
	 * а вид этого «прочего» (см. OTHER_KINDS). Но пак, который весь про спорт,
	 * играется как всякий тематический, и вопрос «а какой именно спорт» у него
	 * ровно тот же, что «какой жанр музыки» у музпака: футбольный пак и пак
	 * про Олимпиады — это два разных вечера.
	 *
	 * Поэтому спорт получает и свой ярлык («Спортпак», см. TOPICS), и свою
	 * полоску жанров — по этому списку. Спрашивается он у тем, у которых вид
	 * «прочего» вышел спортивным (см. THEME_RULES в gemini.js).
	 */
	sport: {
		question: 'Какой это спорт?',
		list: {
			football: 'футбол',
			hockey: 'хоккей',
			basketball: 'баскетбол',
			tennis: 'теннис',
			volleyball: 'волейбол',
			martial: 'единоборства и бокс',
			motorsport: 'автоспорт и гонки',
			athletics: 'лёгкая атлетика',
			winter: 'зимние виды',
			olympics: 'Олимпиады и турниры',
			chess: 'шахматы и настольный спорт',
		},
	},
	/*
	 * Музыкальный список длиннее прочих, и это не прихоть.
	 *
	 * Сначала он был коротким: рэп, рок, метал, электроника, поп и несколько
	 * особых случаев. Беда короткого списка в том, что деться из него некуда:
	 * инди, кантри, панк, соул и R&B попадали в «поп» или в «рок» — не потому,
	 * что модель ошибалась, а потому, что других слов ей не давали. Живой
	 * музыкальный пак после этого выглядел так: «поп 90%», и два совершенно
	 * разных сборника были подписаны одинаково.
	 *
	 * Поэтому список пополнен ровно теми жанрами, которые в живых паках
	 * и встречаются. Порядок, как везде, по старшинству: сперва то, что говорит
	 * о теме больше всего (откуда музыка — из аниме, из игры, из кино), потом
	 * настоящие жанры, и в самом низу «поп» — то, чем вещь оказывается, когда
	 * сказать про неё больше нечего.
	 */
	music: {
		question: 'Какой жанр музыки?',
		list: {
			anisong: 'опенинги и аниме-музыка',
			gaming: 'музыка из игр',
			soundtrack: 'саундтреки кино',
			kpop: 'K-pop',
			rap: 'рэп и хип-хоп',
			metal: 'метал',
			punk: 'панк и эмо',
			rock: 'рок',
			indie: 'инди и альтернатива',
			electronic: 'электроника',
			rnb: 'R&B и соул',
			country: 'кантри',
			jazz: 'джаз и блюз',
			classical: 'классика',
			chanson: 'шансон',
			soviet: 'советская эстрада',
			folk: 'фолк и народное',
			comedy: 'юмор и пародии',
			pop: 'поп',
		},
	},
};

/** У какой тематики жанр вообще спрашивается. Порядок — как в GENRES. */
export const GENRE_TOPIC_KEYS = Object.keys(GENRES);

/**
 * Из чего пак сделан — не по жанру, а по носителю.
 *
 * Вопрос этот стоит у двух тематик, и у обеих он неотменимый. «Манга-пак» —
 * это и японская манга, и корейская манхва, и китайская маньхуа, и вебтуны:
 * рисуются они по-разному, читаются по-разному, и собирают такие паки разные
 * люди. «Кинопак» — это и фильмы, и сериалы, а вечер по сериалам и вечер
 * по фильмам не одно и то же.
 *
 * Раньше обе вещи стояли в списках жанров (webtoon у манги, series у кино),
 * и обе там мешали: жанр отвечает на вопрос «про что оно», носитель — на вопрос
 * «что это вообще», и в одном списке второй вытеснял первый. Пак по корейским
 * вебтунам выходил «манхва, маньхуа и вебтуны 100%», пак по «Сообществу» —
 * «сериалы 60%»; про то, что в них смешное фэнтези и ситком, не говорилось
 * нигде.
 *
 * Поэтому носитель поделён отдельно и показан отдельно — прямо в ВЕРХНЕЙ
 * полоске карточки, там, где стоит сама тематика: у манга-пака кусок «Манга
 * и манхва 100%» распадается на «60% Манга, 30% Манхва, 10% Маньхуа»,
 * у кинопака — на «70% Фильмы, 30% Сериалы». Полоска жанров под ней остаётся
 * жанрам, и на неё же указывает вопрос «Из чего состоит пак?».
 *
 * Имена с большой буквы нарочно: стоят они кусками верхней полоски рядом
 * с «Аниме» и «Играми», а не перечислением внутри жанровой строки.
 */
export const FORMS = {
	manga: {
		question: 'Манга, манхва или маньхуа?',
		list: {
			manga: 'Манга',
			manhwa: 'Манхва',
			manhua: 'Маньхуа',
			webtoon: 'Вебтуны',
			ranobe: 'Ранобэ',
		},
	},
	movies: {
		question: 'Кино или сериалы?',
		list: {
			film: 'Фильмы',
			series: 'Сериалы',
			show: 'Телешоу',
		},
	},
};

/** У каких паков верхняя полоска делится на носители. Порядок — как в FORMS. */
export const FORM_TOPIC_KEYS = Object.keys(FORMS);

/** Есть ли у этого ключа носителя место в списке своей тематики. */
export function isForm(topic, key) {
	return Boolean(key) && Object.hasOwn(FORMS[topic]?.list ?? {}, key);
}

/**
 * Жанры, которые нельзя терять, раз уж они взяли порог.
 *
 * Обычный список жанров обрезается по genreLimit: показывать десяток подписей
 * на карточке незачем, и лишнее отсекается по величине доли. Но отсечь можно
 * и то, ради чего человек эту карточку читает. Меха и махо-сёдзё — жанры,
 * которые в паке либо есть, либо нет: три темы про «Гандам» среди тридцати
 * — это уже мехапак на десятую часть, и знать об этом хочется до игры, даже
 * если рядом стоят шесть жанров покрупнее. «Комедия 30%» такой ценности
 * не имеет: комедия есть почти в каждом аниме-паке.
 *
 * Порог им ставится общий (genreShare, десятая часть пака) — здесь только
 * отменяется обрезка по числу, см. computeGenres в src/topics.js.
 */
export const NOTABLE_GENRES = {
	anime: ['mecha', 'mahou'],
	manga: ['mecha', 'mahou'],
};

/** Стоит ли этот жанр показывать, даже когда список уже полон. */
export function isNotableGenre(topic, key) {
	return (NOTABLE_GENRES[topic] ?? []).includes(key);
}

/** Есть ли у этого ключа жанра место в списке своей тематики. */
export function isGenre(topic, key) {
	return Boolean(key) && Object.hasOwn(GENRES[topic]?.list ?? {}, key);
}

/**
 * Имя уменьшенной копии обложки на хостинге.
 *
 * Формат один для всех — AVIF. Тот же кадр в нём весит вдвое меньше, чем в jpg
 * или webp, а обложки — самое тяжёлое, что отдаёт сайт: их под четыре сотни,
 * и на страницу их разом приходит два десятка. Браузер, который AVIF не понимает,
 * покажет вместо обложки квадрат с первой буквой названия (см. createLogo
 * в web/common.js) — некрасиво, но не сломано; таких браузеров с 2024 года нет.
 *
 * Правило лежит здесь, а не рядом со сборкой, потому что сойтись обязаны двое:
 * scripts/build-web.js под этим именем кладёт файл, cf/src/library.js под этим же
 * даёт на него ссылку. Разойдись они — обложек не будет ни одной.
 *
 * Расширение заменяется, а не приписывается: Content-Type Cloudflare ставит
 * по нему, и «511.jpg.avif» уехал бы как avif с jpg внутри.
 */
export function thumbName(logoFile) {
	return `${logoFile.replace(/\.[^./\\]*$/, '')}.avif`;
}

/**
 * Отпечаток, который приписывается к ссылке на обложку: /logos/thumb/511.avif?v=2.
 *
 * Нужен он из-за промаха, который стоил сайту всех свежих обложек разом.
 * Обложки отдаются из статики с вечным кэшем, и правило это (см. HEADERS
 * в scripts/build-web.js) Cloudflare прикладывает к адресу, а не к файлу:
 * ответ «такой обложки нет» уезжал к браузеру с тем же самым
 * «max-age=31536000, immutable». А «такой обложки нет» по адресу обложки —
 * дело обычное: строка пака уезжает в D1 одной выкладкой, уменьшенная копия
 * появляется следующей, и всякий, кто открыл пак между ними, запоминал пустоту
 * на год вперёд и переспрашивать больше не приходил. Файл давно на месте,
 * а у человека квадрат с буквой — ровно это и было с паками 15915–15917 и 16554.
 *
 * Лечится это двумя вещами сразу, и обе нужны. Заголовок стал честным (сутки
 * вместо года), но до того, кто уже запомнил год, честный заголовок не доходит:
 * он не спрашивает. Достучаться до него можно только другим адресом — вот он.
 * Число ниже меняет адрес всем обложкам разом.
 *
 * Менять его руками больше не придётся: с суточным кэшем промах забывается
 * назавтра сам. Но если когда-нибудь понадобится заставить браузеры спросить
 * заново — прибавьте единицу, и этого хватит.
 */
export const LOGO_VERSION = 2;

/**
 * Ссылка на уменьшенную копию обложки — с отпечатком (см. LOGO_VERSION).
 *
 * Зовут её обе половины проекта, и приходят они разными путями: дома
 * src/server.js даёт ссылку на исходное имя файла (копию считает сам сервер,
 * когда её попросят), на хостинге cf/src/library.js — на имя готовой копии
 * из thumbName. Отпечаток при этом обязан быть один и тот же, поэтому
 * приписывается он здесь, а не в каждом из двух мест по-своему.
 */
export function logoUrl(name) {
	return `/logos/thumb/${name}?v=${LOGO_VERSION}`;
}

export const LEVELS = {
	4: { key: 'easy', name: 'Лёгкий' },
	3: { key: 'medium', name: 'Средний' },
	2: { key: 'hard', name: 'Сложный' },
	1: { key: 'veryHard', name: 'Очень сложный' },
};

/**
 * Тематики, доли которых считает Gemini. Ключи уходят в базу и в интерфейс.
 *
 * Значка здесь нет нарочно: рисует его сайт (web/icons.js), и рисует чертёжом,
 * а не эмодзи. Эмодзи каждая система показывает по-своему — в тёмной вёрстке
 * они выглядят наклейками не в тон, а половина из них ещё и не имеет отношения
 * к тому, что обозначает.
 */
export const TOPICS = {
	anime: { name: 'Аниме', packName: 'Аниме-пак' },
	games: { name: 'Игры', packName: 'Игропак' },
	movies: { name: 'Кино и сериалы', packName: 'Кинопак' },
	cartoons: { name: 'Мультики', packName: 'Мультпак' },
	books: { name: 'Книги', packName: 'Книгапак' },
	// Комиксы и манга живут отдельно от книг и друг от друга нарочно. Раньше всё
	// это лежало вместе: комиксы и манга без экранизации числились книгами, манга
	// с экранизацией — аниме, — и «Книгапак» одинаково означал и Достоевского,
	// и Marvel, и «Берсерк». Играются такие паки совсем по-разному, а по ярлыку
	// это не различалось никак.
	comics: { name: 'Комиксы', packName: 'Комикс-пак' },
	manga: { name: 'Манга и манхва', packName: 'Манга-пак' },
	music: { name: 'Музыка', packName: 'Музпак' },
	// Спорт — ярлык особого рода: доли между собой он не делит (вопрос про футбол
	// числится «прочим», как и был), а достаётся паку, у которого этого «прочего»
	// почти всё и оно спортивное (см. KIND_PACKS и toPrimary в topics.js).
	// Заведён он потому, что «Солянка» про футбольный пак — неправда: солянкой
	// он числился не по содержимому, а потому, что спорту негде было стать
	// тематикой. Внутри у него свои виды спорта (см. GENRES.sport), а пак,
	// который целиком про один футбол, вдобавок сохраняет и мишень «Футбол».
	sport: { name: 'Спорт', packName: 'Спортпак' },
	other: { name: 'Прочее', packName: 'Солянка' },
};

/**
 * Виды «прочего», которые дорастают до собственного типа пака.
 *
 * Ключ — вид из OTHER_KINDS, значение — ярлык из TOPICS. Список нарочно
 * короткий: тип пака должен что-то обещать вечеру, а «Историопак» и «Едапак»
 * не обещают ничего — их место по-прежнему в подписи «Солянка (история)».
 * Спорт же обещает: под футбольный пак собирается своя компания.
 */
/**
 * Ярлыки, которых модель не ставит: их считает порог, а не разметка.
 *
 * «Солянка» — пак, в котором ни одна тематика не набрала своей доли
 * (см. toPrimary в topics.js), «Без разметки» — пак, которого разметка ещё
 * не касалась вовсе. В базе они лежат в том же столбце primary_topic, что
 * и обычные ярлыки, и всякий, кто зовёт пак по имени, обязан знать про них
 * тоже — иначе шесть тысяч солянок остаются безымянными.
 *
 * Ровно этот список повторён в web/card.js (EXTRA_TOPICS): там его читает
 * браузер, здесь — сервер, и одолжить оттуда нечего.
 */
export const EXTRA_TOPICS = {
	mixed: { name: 'Солянка', packName: 'Солянка' },
	unknown: { name: 'Без разметки', packName: 'Без разметки' },
};

/**
 * Как называется пак этого ярлыка: «Аниме-пак», «Солянка». Пустой ответ —
 * ярлык неизвестен, и звать пак по имени не следует вовсе.
 */
export const packNameOfTopic = key => (TOPICS[key] ?? EXTRA_TOPICS[key])?.packName ?? null;

export const KIND_PACKS = { sport: 'sport' };

/**
 * Спорт назван ключом отдельно, потому что живёт сразу в трёх списках и должен
 * во всех трёх называться одинаково: это и вид «прочего» (OTHER_KINDS), и тип
 * пака (TOPICS), и список жанров (GENRES) — виды спорта внутри спортпака.
 */
export const SPORT_KEY = 'sport';

/**
 * Виды «прочего» — то, чем это «прочее» на самом деле оказывается.
 *
 * Пять основных тематик отвечают на вопрос «откуда вопрос», и всё, что не аниме,
 * не игры, не кино, не мультики и не книги, сваливается в общую кучу. Куча эта
 * большая и разная: пак про стримеров и пак про Вторую мировую в ней неразличимы,
 * хотя играются совсем по-разному.
 *
 * Поэтому у каждой темы «прочего» модель называет ещё и вид (см. поле k в
 * src/gemini.js), а сайт пишет его рядом с «Прочим», когда вида набралось
 * заметно много (см. otherKindShare). Список закрытый нарочно: открытый превратился
 * бы в тысячу написаний одного и того же, а нужен он ровно затем, чтобы куски
 * складывались в доли.
 */
export const OTHER_KINDS = {
	streamers: 'стримеры',
	bloggers: 'ютуберы и блогеры',
	memes: 'мемы и интернет',
	history: 'история',
	science: 'наука',
	geography: 'география',
	sport: 'спорт',
	celebrities: 'знаменитости',
	tech: 'техника и IT',
	// Оружие и армия — своим видом, а не внутри «техники и IT». Раньше автомат,
	// танк и линкор попадали именно туда, и пак про стрелковое оружие выходил
	// «техника и IT на 84%» — то есть подписывался словом, которое человек читает
	// как «компьютеры и гаджеты». Это разные паки и разные компании под них,
	// и складывать их в одну кучу нельзя: «техника» здесь про бытовую и цифровую,
	// а всё военное — сюда
	weapons: 'оружие и армия',
	art: 'искусство',
	food: 'еда',
	life: 'быт и общество',
};

export const OTHER_KIND_KEYS = Object.keys(OTHER_KINDS);

/**
 * Спецвопросы: как называются по-русски те виды, что встречаются в паках.
 * Ключи ставит разбор content.xml (см. siq.js), а имена нужны только сайту.
 */
export const SPECIAL_NAMES = {
	auction: 'Аукцион',
	cat: 'Кот в мешке',
	noRisk: 'Без риска',
	forAll: 'Вопрос для всех',
	other: 'Прочие особые',
};

/**
 * Языки паков. В самом файле язык записан как «ru-RU» или «en-US», и сайт
 * сводит его к первым двум буквам: разбирать «ru-RU» и «ru» по отдельности
 * не за чем. Языка может не быть вовсе — это ключ unknown.
 *
 * Записанному в файле верить, однако, нельзя: поле это ставит редактор, а не
 * автор, и стоит в нём по умолчанию то, какая у автора Windows. Английских
 * паков в базе по этому полю выходило больше, чем их есть на свете, а половина
 * базы числилась «без указания». Поэтому язык называет ещё и модель — по тому,
 * на каком языке написаны вопросы, темы и ответы (см. LANGUAGE_RULES
 * в src/gemini.js), и сайт предпочитает её ответ (см. language_ai).
 */
export const LANGUAGE_NAMES = {
	ru: 'Русский',
	en: 'English',
	uk: 'Українська',
	be: 'Беларуская',
	kk: 'Қазақша',
	de: 'Deutsch',
	fr: 'Français',
	es: 'Español',
	pl: 'Polski',
	ja: '日本語',
	tr: 'Türkçe',
	unknown: 'Без указания',
};

/** Языки, которые модели разрешено называть: «unknown» — это не язык, а его отсутствие. */
export const LANGUAGE_KEYS = Object.keys(LANGUAGE_NAMES).filter(key => key !== 'unknown');

/**
 * Категории, которые делят вопросы пака между собой: у каждой темы ровно одна,
 * и доли в сумме дают сто процентов.
 */
export const EXCLUSIVE_TOPIC_KEYS = ['anime', 'manga', 'games', 'movies', 'cartoons', 'books', 'comics', 'other'];

/**
 * Музыка стоит особняком: она отвечает не на вопрос «о чём тема», а на вопрос
 * «как её отгадывают». Тема с опенингами — это и аниме, и музыка сразу, поэтому
 * доля музыки считается поверх остальных и в сотню не входит: пак вполне бывает
 * аниме-паком на 100% и музыкальным на 90%.
 */
export const MUSIC_KEY = 'music';

/** Все ключи, встречающиеся в topic_shares. */
export const TOPIC_KEYS = [...EXCLUSIVE_TOPIC_KEYS, MUSIC_KEY];
